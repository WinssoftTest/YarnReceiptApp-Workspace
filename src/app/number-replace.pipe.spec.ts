import { NumberReplacePipe } from './number-replace.pipe';

describe('NumberReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
